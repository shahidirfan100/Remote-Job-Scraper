{
    "actorOutputSchemaVersion": 1,
    "title": "Remote.co Jobs Scraper Output",
    "properties": {
        "dataset": {
            "type": "string",
            "title": "Job Listings",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}