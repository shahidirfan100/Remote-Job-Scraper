{
  "title": "Remote.co Jobs Scraper",
  "description": "Extract remote job listings from Remote.co with customizable search options.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Search URL (Optional)",
      "type": "string",
      "description": "Remote.co search URL. If provided, keyword and location will be ignored.",
      "editor": "textfield",
      "pattern": "^https?://.*remote\\.co.*$"
    },
    "keyword": {
      "title": "Search Keyword",
      "type": "string",
      "description": "Job title or skill (e.g., 'Software Engineer', 'Marketing Manager').",
      "editor": "textfield",
      "prefill": "software engineer"
    },
    "location": {
      "title": "Location (Optional)",
      "type": "string",
      "description": "Filter by location. Leave empty for worldwide remote jobs.",
      "editor": "textfield"
    },
    "results_wanted": {
      "title": "Maximum Jobs",
      "type": "integer",
      "description": "Maximum number of job listings to collect.",
      "minimum": 1,
      "maximum": 10000,
      "default": 50,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum Pages",
      "type": "integer",
      "description": "Maximum number of search result pages to process.",
      "minimum": 1,
      "maximum": 100,
      "default": 10,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings for reliable scraping.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      }
    }
  },
  "required": []
}